{% import "_includes/forms" as forms %}

<input type="hidden" name="sproutBaseSettingsType" value="{{ className(settings) }}">

{{ forms.textField({
    label: "Plugin Name"|t('sprout-base-sent-email'),
    instructions: "Intuitive, human-readable plugin name for the end user."|t('sprout-base-sent-email'),
    id: 'pluginNameOverride',
    name: 'pluginNameOverride',
    value: settings.pluginNameOverride,
    placeholder: 'Sprout Sent Email',
    autofocus: true,
    first: true,
    errors: ''
}) }}